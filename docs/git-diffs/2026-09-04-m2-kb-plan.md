# Git Diff 总结：M2 T2.2 知识库规划与准备工具

- 日期：2026-09-04
- 里程碑：M2 平台与基础集成，子任务 T2.2

## 涉及文件

### 新增

- `docs/knowledge-base-plan.md`：标准规程库/台账与记录库/图片证据库三库规划、分块与摘要策略、权限、检索验收口径
- `scripts/load_knowledge_bases.py`：KB manifest 生成、CSV 表格 Markdown 摘要、文件清单校验工具
- `tests/test_kb_manifest.py`：分类、manifest 文件存在性、表格摘要测试
- `data/generated/kb_manifest.json`、`data/generated/03_quality_records/table_summaries/*.md`（生成产物，不入库）

### 修改

- `README.md`：T2.2 完成状态与里程碑同步

## 变更要点

1. 知识库按语义分区：标准规程库（5 文件）、台账与记录库（16 文件，含 5 个表格摘要）、图片证据库（3 文件）。
2. CSV 台账先转结构化 Markdown 摘要，提高表格检索命中；原始 CSV 保留为附件可追踪。
3. 输出 `kb_manifest.json`，平台可用后按清单上传并做 Hit@5 验收。
4. 依据官方“知识库配置”文档修正图片策略：PNG 未列入知识库上传格式清单，标记为
   `ingest=false`，由 VLM 生成图片事实文本后再入库；文档补充官方站点依据链接。
5. 修复 manifest 二次生成时表格摘要重复计数问题：先生成摘要、再统一分类。

## 测试与验收

- 34 项 `unittest` 全部通过（新增 3 项 T2.2 测试）。
- 实际运行生成 kb_manifest：3 个知识库、合计 24 个文件，全部校验存在且无重复。

## 注意事项 / 影响

- 知识库实际导入与检索指标依赖 Nexent 平台模型配置（当前无 API Key）。
- 生成产物位于 gitignore 的 `data/generated/`，新环境按 README 命令复现。
